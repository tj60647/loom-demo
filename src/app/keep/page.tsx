// 05 · Keep lives on the shelf, not inside a reading: the export is the whole
// artifact and a student's work is never a slice of itself (red line #5).

import KeepPage from "@/components/shelf/KeepPage"

export default function Page() {
  return <KeepPage />
}
